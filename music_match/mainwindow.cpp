/*#include "mainwindow.h"


MainWindow::MainWindow(QWidget *parent)
    : QMainWindow(parent)
    , ui(new Ui::MainWindow)
{
    ui->setupUi(this);

}

MainWindow::~MainWindow()
{
    delete ui;
}


void MainWindow::on_pushButton_clicked()
{
    QString username = ui->username_bakas->text();
    std::string Username = username.toUtf8().constData();
    if(username !=""){
        //connection();
        //checking all usernames stored in databass.
        //userProfile_check();
        //username_check:
        /*for (int i = 0; i < last_row_id; i++) {
               if (username == Profile_names[i]) {
                   QMessageBox::information(this,"login","it seems your username already exists...\n
                    please use another username.");
                   goto username_check;
               }
           }

        //userName(Username);
        hide();
        //second = new second_page(this);
        //second->show();
    }
    else QMessageBox::information(this,"login","please enter a username.");
}
*/
