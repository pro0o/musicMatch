#ifndef LAST_PAGE_H
#define LAST_PAGE_H

/*
#include <QDialog>
#include <database.h>


namespace Ui {
class last_page;
}

class last_page : public QDialog
{
    Q_OBJECT

public:
    explicit last_page(QWidget *parent = nullptr);
    ~last_page();

private:
    Ui::last_page *ui;
    last_page* last;
};

*/
#endif // LAST_PAGE_H

